#include <stdio.h>
#include <stdlib.h>

void main(){

    char temp = "It isn't too hot outside today, probably around 72 degrees. It is quite nice.\n"
    char humidity = "It actually isn't very humid right now, I guess it hasn't rained in a while\n"
    char rain = "It is not raining right now and I don't expect it too rain soon\n"

    printf(temp + humidity + rain)
}
